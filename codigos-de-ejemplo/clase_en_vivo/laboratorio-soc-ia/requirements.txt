# ── Dependencias del laboratorio (todo Python) ──────────────────────────
# Se instalan en las imágenes custom: mcp-server, soar-bridge y jupyter.

# MCP (Model Context Protocol): servidor y cliente. Serie 2.x (API MCPServer).
mcp>=2.0,<3

# API web del SOAR-bridge.
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9      # necesario para los formularios (aprobar/rechazar)

# Modelos y configuración.
pydantic>=2.6
pydantic-settings>=2.2

# Cliente HTTP (backend OpenAI y utilidades).
httpx>=0.27

# Cliente de Ollama (backend LLM local, opcional).
ollama>=0.3

# Anomaly ML POC (Isolation Forest tabular).
scikit-learn>=1.4
joblib>=1.3
numpy>=1.26

# Ataque SSH real.
paramiko>=3.4

# Notebook y análisis interactivo.
jupyterlab>=4.0
pandas>=2.2
rich>=13.0
requests>=2.31
tabulate>=0.9
